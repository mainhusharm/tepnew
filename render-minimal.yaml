services:
  - type: web
    name: flask-test-app
    env: python3
    plan: starter
    buildCommand: pip install Flask==2.2.2
    startCommand: python3 simple_app.py
    envVars:
      - key: PORT
        value: 10000
